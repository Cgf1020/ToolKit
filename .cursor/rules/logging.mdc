---
description: 通用日志规范：级别、热路径、生命周期、脱敏与统一接口
globs: "**/*.{c,cc,cpp,cxx,h,hpp,hxx}"
alwaysApply: false
---

# 日志规范

只用本工程统一的日志接口（`LOG_DEBUG` / `LOG_INFO` / `LOG_WARN` / `LOG_ERROR`）。禁止 `std::cout` / `std::cerr`、禁止第二套日志库、禁止用打印当断言。

## 级别

- **Info**：模块创建/初始化/启动/停止/销毁；状态迁移（`from → to`）；外部控制指令；连接通断；现场默认就要看到的业务事件（如告警上报）。
- **Debug**：热路径过程、周期统计、心跳、排障细节。现场默认关闭。
- **Warn**：可恢复失败、非法调用、校验失败、将重试的首次失败。
- **Error**：本次操作失败、初始化失败、无法继续。不要把“还能跑的异常”打成 Error。

## 热路径

禁止在逐帧、逐包、逐样本、循环每次迭代时打印 Info。过程日志不打或只打 Debug。

同类错误禁止每帧/每包一条 Warn/Error：第一次可以打，之后限流或按时间窗汇总。

帧率、计数、队列、丢包等统计用 Debug，且按时间窗汇总，禁止 Info 刷屏。

```cpp
// BAD: 每帧 Info
LOG_INFO("frame ts={}", ts);

// GOOD: 周期汇总走 Debug
if (stats_due) {
    LOG_DEBUG("frames={} drops={}", n, d);
}
```

## 控制、生命周期、业务事件

- 状态**变化**、控制指令、连接**通断**：Info。心跳/轮询：Debug 或限流。
- 对外 Init/Start/Stop/Release：Info，失败 Error。内部临时对象不要层层打构造/析构。
- 告警/上报默认 Info；同类短时间连续触发时限流或带计数。

## 内容

一条日志一件事、单行。失败带现场（对象、返回值、路径、原因），优先 `key=value`，同类关键字固定。

禁止打印密码、token、密钥、完整连接串；配置打印必须脱敏。禁止二进制、整帧图像、超大 dump。

日志库已提供文件/函数/行号时，不要再手工加模块名前缀。跨模块分流用 `LOG_M_INFO("communication", ...)` 或 `Logger::get("communication")`。

信号处理器里不用日志宏。析构只打必要生命周期，且不访问已释放对象。
